

1. session 如何共享（所谓单点登录）
我们已经实现了 session共享

把session放在后端的某一公共地方（如数据库），以统一管理。
有的是用redis存的，有的是用数据库存储的。
使用一串码（ticket）来唯一标识某一用户！

2. 需要权限控制时，可以使用拦截器实现
有没有登录
某些课程是否付费了

3. status 的设计思路
网站上的数据都是很重要的，尽量不删除！
旧的失效，新的加进来。

4. 敏感词过滤
将敏感词构造为一棵前缀树
TrieNode

5. 多线程
任务执行框架
Executor



